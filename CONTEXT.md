# 작업 히스토리

지금까지 무엇을, 왜 그렇게 만들었는지. 코드만 봐서는 알 수 없는 배경입니다.
(마지막 갱신: 2026-08-01)

---

## 1. 브랜드

서울 기반 DJ 크루. 2026년 시작. 지금은 창립 멤버 모집 단계입니다.

- **슬로건** — WHERE THE LIGHTS FADE, THE MUSIC TAKES OVER.
- **톤** — 흑백, 여백 넓게, 프리미엄. 기준으로 삼은 표현은 **"국내 동호회 느낌이 아니라 해외 DJ 브랜드처럼"**. 이게 모든 디자인 판단의 기준입니다.
- **키워드 8개** — UNDERGROUND / ENERGY / UNITY / FUTURE / HOUSE / TECHNO / MINIMAL / NIGHT
- **인스타** `@blackoutcrew_official` · **메일** ujin141@naver.com · **도메인** https://www.blackoutsound.com

---

## 2. 멤버

정렬 순서는 사용자가 직접 지정한 것입니다 — **V · LYNN · AROS · TS**. 임의로 바꾸지 마세요. DEMIC은 맨 뒤에 붙였습니다.
2026-08-01에 **AKILL이 크루를 나가 전부 삭제**됐습니다. 사이트·제안서·카드뉴스·누끼 이미지·`cards.py` 항목 모두 지웠습니다. 다시 넣을 일이 있으면 커밋 `9f6ef94` 이전을 보세요.
정확한 데이터는 `assets/js/members.js`가 원본입니다. 아래는 배경만.

| DJ | 메모 |
|---|---|
| **V** | 30살. 자기소개 문구는 사용자가 직접 준 문장이라 **바꾸지 마세요** — "남녀불문, 장르에 국한되지 않게 음악의 길로 안내해 드립니다." |
| **LYNN** | 사운드클라우드 있는 유일한 멤버(+AROS) |
| **AROS** | 경력에서 **개월 수는 빼달라**는 요청이 있었습니다. Alan Walker로 입문한 이야기가 소개에 들어감 |
| **TS** | 크루 내 디자이너 역할도 겸합니다. 사운드클라우드 미확보 |
| **DEMIC** | 2026-08-04 합류. 경력은 본인 프레스킷(`DEMIC/DJ-DEMIC-PRESSKIT_2025_ver_.pdf`, 이미지 PDF라 텍스트 추출 안 됨)에서 가져왔습니다. **장르 정보 없음 — 물어봐야 함.** 프레스킷에 전화번호·개인 메일이 있으나 사이트에는 넣지 않았습니다 |

**사진 처리** — 원본 사진은 개인정보(프로필 카드에 전화번호)가 있어 저장소에 없습니다. rembg(`birefnet-general` 등)로 배경을 뺀 결과물만 `assets/img/members/`에 있습니다.
V의 원본은 머리 윗부분이 잘려 있어서, **누끼 상단 14%에 알파 그라데이션**을 넣어 머리카락이 자연스럽게 사라지게 처리했습니다. 다시 딸 때 이 처리를 빠뜨리면 머리가 평평하게 잘려 보입니다.

---

## 3. 사이트

`index.html` 한 장. 섹션: Hero → About → Philosophy → What We Do → Gallery → Join → Contact → Footer.

- **한국어 기본**, 우측 상단에서 EN 전환. 문구는 전부 `i18n.js`.
- **갤러리·배경에 스톡 사진을 쓰지 않습니다.** 공연 사진이 없어서 조명·헤이즈·관객 실루엣을 캔버스로 그립니다(`main.js`의 `ART`). 실제 사진이 생기면 `<figure class="tile">` 안에 넣으면 그림 대신 표시됩니다.
- **히어로 로고는 3D**입니다. 엠블럼·글자·광원을 층으로 쌓고 마우스를 따라 기울며, 시차·금속 반사광·바닥 조명이 함께 움직입니다. 층 위치 퍼센트 값은 원본 로고에서 계산한 것이라 건드리면 어긋납니다.
- **한글 타이포** — 브랜드 폰트 Michroma에 한글이 없어 Pretendard로 폴백됩니다. 그래서 `html[lang='ko'] .display` 크기를 따로 잡고 `word-break: keep-all`을 씁니다.

### SEO / AEO / GEO

멤버 카드가 JS로만 그려져서 **크롤러와 AI 답변엔진에 안 보이는 문제**가 있었습니다. 해결:
JSON-LD `@graph`(MusicGroup·WebSite·WebPage·FAQPage) + `<noscript>` 멤버 목록 + `llms.txt` + `robots.txt`에 AI 크롤러 명시 허용.
그래서 **멤버 수정 시 네 곳을 함께 고쳐야 합니다**(CLAUDE.md 참고).

구글·네이버 사이트 소유확인 태그가 `index.html` head에 들어 있습니다. 지우지 마세요.

---

## 4. 영상 (`video/`)

**음악과 효과음을 전부 코드로 합성합니다** — numpy + scipy로 킥 피치드롭, 슈퍼소우, 애시드 스윕, 사이드체인 덕킹, 컨볼루션 리버브를 직접 만듭니다. 저작권 이슈를 피하기 위한 선택입니다.
렌더는 numpy/cv2로 프레임을 그려 ffmpeg에 rawvideo로 파이프합니다. 전부 **1080×1920 숏폼**.

| 파일 | 내용 |
|---|---|
| `audio.py` + `make.py` | 티저 28초. 128BPM, 드롭 15.0s. 타이포·모션그래픽만, **사람 사진 없음**(사용자 요청) |
| `audio_open.py` + `opening.py` | 오프닝 1. 128BPM 30초, 드롭 22.5s. 레이더 와이프 엠블럼 등장, 03/02/01 카운트다운, 관객 실루엣 |
| `audio_open2.py` + `opening2.py` | 오프닝 2. **140BPM 하드테크노** 29초. 터널 컨셉(소실점·사각 링·방사선), 16분음마다 흑백 반전 스트로브. 오프닝 1과 시각 언어가 겹치지 않게 만든 것이라 빛기둥·관객 실루엣을 쓰지 않습니다 |
| `audio_open3.py` + `opening3.py` | 오프닝 3. **174BPM 브레이크비트/DnB** 29초. 4온플로어를 쓰지 않는다(킥 1박·스네어 3박). 오실로스코프 컨셉 — **실제로 만든 wav를 읽어 파형·리사주·FFT 막대를 그린다.** 13마디는 통째로 **흑백 반전(흰 바탕)**. 1·2와 겹치지 않게 빛기둥·헤이즈·소실점·관객을 전부 안 쓴다 |
| `cards.py` | 인스타 카드뉴스. DJ 1명당 5장 |
| `feed.py` | 3240×1350 파노라마를 1080×1350 세 장으로 분할. 프로필 그리드에서 잘리므로 핵심은 y 135~1215 안에 |
| `feed2.py` | 이어지는 4·5번 두 장. **가로선 y=1058과 바닥 그라데이션을 첫 세트와 똑같이 맞춰야 이어져 보인다.** 엠블럼은 두 장의 이음새에 걸침 |
| `audio_poster.py` | **포스터 영상 전용 음원 다섯 곡.** 처음엔 릴스 곡을 그대로 물렸다가 "예전 영상이랑 BGM이 같다"는 지적을 받았다 — **같은 곡을 다른 영상에 또 쓰면 계정 전체가 같은 영상처럼 들린다.** 장르·BPM·조성을 릴스와 전부 갈랐다: afro 122 · industrial 138 · garage 136 · synthwave 118 · breaks 110 (릴스는 128·145·132·155·105). 소리 부품은 `audio_reel.py` 에서 가져다 쓰고 리듬 골격만 새로 짰다 |
| `audio_reel.py` + `reel_word.py` | 멤버별 키워드 릴스 5편(15초). **BPM만 바꾸면 다 비슷하게 들린다** — 처음에 그렇게 만들었다가 "비트가 다 같다"는 지적을 받았다. 실제로 갈리려면 하이햇 분할·스네어 위치·베이스 싱코페이션·조성까지 달라야 한다. 지금은 스타일마다 리듬 골격을 따로 짬 — festival 128 / techno 140 / bounce 128 / hard 150 / citypop 110(드롭 없음). `audio.sub()`는 음이름을 받으므로 주파수용 `subf()`를 따로 씀. 커버는 `feed_row`의 `{key}_3.png` |
| `feed_row.py` | **피드 한 줄(3칸)이 통째로 이어지는 세트.** 왼쪽=멤버(카드뉴스 1번 슬라이드), 가운데=릴스 커버, 오른쪽=브랜드. 3240×1350을 세 장으로 자름. 이름은 `min(fit(...), 118)`로 상한을 둬야 V·TS가 인물을 덮지 않는다 |
| `make_og.py` | 링크 미리보기용 1200×630 카드 |
| `poster_split.py` | **디제잉 풀파티 × 솔로파티 최종안.** 물 사진만 쓰면 그냥 수영장 행사로 읽혀서 위(물)/아래(클럽 조명·디스코볼)로 나눴다. 경계에 ×를 물려 두 개가 만나는 자리로. 사진은 `duotone()`으로 두 색 사이에 다시 칠해 어떤 사진을 넣어도 톤이 안 튄다. **처음 판은 "촌스럽다"고 반려됐다** — 원인과 대책은 아래 참조 |
| `poster_club.py` | **같은 행사 B안 — 클럽 파티 판.** A안과 나란히 놓고 고르라고 만든 것이라 **일부러 겹치는 장치를 안 쓴다**: 사진 한 장 · 직각 격자(A안은 7도 기울임) · 세로로 쌓은 대형 타이포 · 검정×레드(A안은 시안×마젠타) · 스캔라인·스트로브(A안은 섬광·보케) · 라벨 위/값 아래 카드형(A안은 한 줄 표). 낱말을 전부 같은 폭에 맞추면 세로가 넘치고, 안 넘치게 줄이면 오른쪽이 빈다 — **큰 낱말은 전폭 왼쪽, 작은 낱말은 58% 폭 오른쪽**으로 갈라서 둘 다 푼다 |
| `poster_ticket.py` | **C안 — 입장권.** 다섯 중 **유일하게 밝은 판**이다. 피드가 대부분 어두운 사진이라 흰 종이 한 장이 스크롤에서 제일 먼저 걸린다. 티켓으로 읽히려면 넷이 다 있어야 한다: 절취선+양 끝 홈 · ADMIT ONE/일련번호 · 점선 리더 조판 · 바코드. **바코드는 장식이고 아무 정보도 없다** |
| `poster_neon.py` | **D안 — 네온 사인.** 글자를 채우지 않고 속을 비운 테두리로 그린 뒤 빛을 더한다. 튜브처럼 보이려면 **세 겹**이 필요하다 — 넓고 옅은 후광, 좁고 진한 후광, 흰빛에 가까운 심. 하나라도 빠지면 그냥 외곽선이다. 바닥 반사(위아래 뒤집은 흐린 복사본)를 옅게 깔아야 간판이 걸린 것처럼 읽힌다 |
| `poster_grid.py` | **E안 — 모듈 그리드.** 사진 위에 글자를 얹지 않고 판을 칸으로 쪼갠다. 정보 밀도가 제일 높아 라인업까지 한눈에 보여야 할 때 쓴다. **칸 높이는 픽셀이 아니라 비중(weight)으로 잡을 것** — 피드와 스토리는 세로가 1.42배 차이 나서 픽셀로 박으면 한쪽이 남거나 넘친다 |
| `poster_motion.py` | **포스터 다섯 시안의 영상판** (스토리 1080×1920 · 피드 1080×1350 · 30fps · BGM 포함). 사이즈마다 `story` 플래그가 갈리므로 **경계 위치·바코드 위치 같은 상수를 포스터 쪽과 똑같이 맞춰야 한다** — 어긋나면 마퀴가 경계에서 뜨고 스캐너 선이 바코드를 비껴간다. **매 프레임 다시 그리지 않는다** — `build()` 한 번이 1~2초라 450프레임을 다시 그리면 시안 하나에 10분이 넘는다. 정지본을 한 장 만들고 그 위에 시간축 처리를 얹는다. 예외는 A안 마퀴 띠 — 실제로 흘러야 해서 프레임마다 다시 얹되 띠는 캐시하고 `np.roll` 로만 민다. 움직임은 시안의 디자인 언어를 따른다: 물결/스트로브/빛 스침/점멸/칸 등장 |
| `event.py` | **행사 정보 원본.** 날짜·시간·타임테이블·가격·협업 브랜드가 전부 여기 있고 시안 다섯이 가져다 쓴다. 전에는 시안마다 같은 값을 복사해 뒀는데 다섯으로 늘면서 날짜 하나 바꿀 때마다 다섯 군데를 고쳐야 했고 한 곳만 빠뜨려도 티가 안 났다. **라인업은 타임테이블에서 뽑는다** — 따로 적으면 둘이 어긋난다 |
| `poster_kit.py` | 포스터 공통 도구. 시안이 다섯으로 늘면서 한 곳만 고쳐도 나머지가 어긋나기 시작해 뺐다. **C·D·E안만 쓴다** — A·B안은 이미 값이 미세 조정돼 있어 건드리지 않았다 |
| `poster_photo.py` | **F안 — 실사 물 사진, 밤.** 타이틀이 안 보인다는 지적으로 다시 짰다. 원인은 밝은 수면 위에 흰 글자를 얹고 안 보이니 그림자를 덧댄 것 — **그림자는 대비를 못 만들고 글자만 지저분하게 한다.** 사진을 눌러 글자 자리를 먼저 만든다. 행사가 19시~24시라 밤으로 갔다: 듀오톤을 짙은 남색으로 · 수중 조명 · 비네트. **수중 조명이 없으면 그냥 어두운 물이지 밤이 아니다** |
| `band.py` | **입장 밴드(밴딩) 인쇄 원고 4종** (GUEST · VIP · VVIP · STAFF). **바탕은 흑백, 색은 VIP·VVIP 에만.** 포스터에 준 컬러 예외는 행사 모객용에만 해당하고, 밴드는 손님이 행사 내내 차고 다니는 **브랜드 접점**이라 크루 톤(흑백·여백·미니멀)을 따른다. 다만 **GUEST(검정)·STAFF(흰색)를 무채색으로 두고 가운데 둘에만 색을 얹으면 색 자체가 '특별함'을 뜻하게 된다** — 넷 다 색을 주면 알록달록한 밴드가 되고 등급 의미가 사라진다. 색은 행사 이름에서 가져왔다(AFTER SUNSET — VIP 는 노을의 붉은 끝, VVIP 는 금빛). 그래도 색만 믿지 않는다: **밝기 네 단**(0.11/0.43/0.69/0.89)과 **세는 막대**(1·2·3·4)를 그대로 둔다. 날짜도 뺐다 — 하루짜리 행사의 밴드에 날짜를 적는 건 자기 자신을 설명하는 것이다. 남은 건 이름·등급·협업 브랜드 셋뿐. 3000×300px = 254×25mm @300dpi. 밴드는 포스터가 아니라 25mm 띠라 규칙이 따로다. ① 손목에 감으면 3분의 1도 안 보여 같은 덩어리를 **두 번 반복**한다. ② **위계는 셋까지** — 이름/형식/날짜·시간. 장소·가격·핸들까지 넣으면 25mm 안에서 전부 같은 크기가 되고 그러면 아무것도 안 읽힌다. **뺄 것을 정하는 게 이 판의 설계다.** ③ 재단 여유 ±1mm 를 먹고 들어가므로 위아래 4mm(`SAFE`)는 비운다. ④ 좌우 두 블록은 **베이스라인을 공유**한다. ⑤ 가르는 세로선은 고정 좌표가 아니라 남는 폭 한가운데 — 글자 길이가 바뀌면 한쪽에 붙는다. ⑥ 위아래 선은 캔버스 전체에 한 번에 긋는다(덩어리마다 그리면 이음새에서 1px 어긋난 게 줄로 보인다). ⑦ **위아래 가로 괘선은 뺐다** — 가장자리와 나란해서 재단선으로 오해를 샀다. 뺀 자리에 협업 브랜드 줄이 들어갔다(장식을 정보로 바꾼 셈). 협업 브랜드는 **제일 작게** — 넣어야 하는 정보지만 이름보다 커지면 밴드가 협찬 스티커가 된다. 폭이 남는다고 키우면 위계가 뒤집힌다. ⑧ **등급 색은 색상환에서 멀리 떨어뜨린다** — 남색·청록처럼 이웃한 색은 조명 아래에서 같아 보인다. 파랑(220°)·분홍(335°)·호박(42°). ⑨ **등급은 문에서 제일 먼저 확인하는 정보다** — 처음엔 밴드에서 제일 작은 글자였다. 이름·날짜보다 작으면 기능을 못 한다. 칩 글자를 23 → 30 으로 키우고 날짜를 32 → 27 로 내려 자리를 만들었다(이름 > 등급 > 날짜 > 협업). ⑩ **등급 낱말 길이가 다르면 이름 크기를 역산한다** — GUEST/VIP/VVIP/STAFF 는 칩 폭이 제각각이라 이름을 고정 크기로 두면 넓은 등급에서 날짜를 밀고 들어간다. 오른쪽(칩→날짜)부터 자리를 잡고 남는 폭에 이름을 맞춘다. ⑩ **등급 표기는 영문으로 통일** — '일반'·'직원'을 섞으면 밴드가 안내문이 된다. ⑪ **등급을 색으로만 나누지 않는다** — 밤 행사라 클럽 조명 아래에서 색이 뒤집히고(붉은 조명에서 호박색은 흰색으로 보임) 색약도 있다. **세는 막대**를 넣어 GUEST 1 · ARTIST 2 · STAFF 3 으로 만들었다. 배경 밝기도 세 단으로 벌려(0.14 / 0.25 / 0.69) 흑백으로 봐도 갈린다 |
| `poster_loud.py` | **브랜드 흑백 규칙을 일부러 깬 유일한 자산** (사용자가 명시적으로 요청). 흑백으로는 "풀파티"가 즉각 안 읽힌다 — 물색·햇빛·튜브가 곧 정보다. 크루 브랜드용이 아니라 **행사 모객용**이므로 다른 자산과 톤을 맞추려 하지 말 것 |
| `poster_ad.py` | 같은 행사 **판매용**. 티저와 역할이 다르다 — 예쁜 여백보다 빠짐없는 정보가 우선. 위계는 레퍼런스 공통안을 따랐다: **행사명 → 날짜·장소 → 특징(성비·포함·정원) → 가격 → CTA**. 세로 위치는 반드시 H 비율로 잡을 것(오프셋 누적하면 칸이 서로 먹는다). 한글은 KR/KRB로 — BRAND(Michroma)에 한글이 없어 네모로 깨진다 |
| `poster_solo.py` | 풀파티 × 솔로파티 전용. **검정 일색으로 가면 언더그라운드 클럽 포스터가 되어 컨셉이 죽는다** — 낮·물·밝음으로 뒤집었다. 컨셉 장치는 **겹치는 두 원**(혼자 온 사람 둘이 만나는 지점, ×가 교점에 놓임). 원이 화면 밖으로 나가면 하나의 렌즈처럼 보여 컨셉이 안 읽히므로 `R=W*0.285`를 넘기지 말 것. **"혼자 와도 됩니다" 한 줄이 이 포스터의 핵심** |
| `poster_event.py` | 행사 포스터. **화려함은 방사선+동심원 한 덩어리로만 낸다** — 장식을 늘리면 작은 화면에서 뭉갠다. `disc()`로 가운데를 눌러 글자 자리를 만든 뒤 큰 타이포를 얹는다. 피드(4:5)·스토리(9:16) × 어두운/반전 4종 |
| `poster_pool.py` | 인스타 스토리 포스터 1080×1920 (풀파티 × 솔로파티). 물결(코스틱)을 사인파 간섭으로 그림. **날짜·장소는 파일 맨 위 상수만 고치면 됨** |
| `kakao_bg.py` | 카톡 단톡방 배경 1440×3120 두 종. **말풍선이 위에 얹히므로 로고 알파를 0.08~0.20으로 아주 낮게** 쓴다. 상단 320px·하단 420px은 카톡 UI가 덮으니 비워 둔다 |

**시안은 색만 바꾸지 않습니다.** 다섯 개를 나란히 놓고 고르는 용도라 **구조·타이포·색·질감·정보 배치를 전부 겹치지 않게** 짰습니다. 색만 다른 변주는 고를 이유가 없어 시안이 아닙니다.

| | 구조 | 타이포 | 색 | 질감 |
|---|---|---|---|---|
| A 물×클럽 | 사진 두 장 · 7도 기울임 | 가로 두 줄 | 시안×마젠타 | 물빛 섬광 · 보케 |
| B 클럽 | 사진 한 장 · 직각 격자 | 세로로 쌓음 | 검정×레드 | 스캔라인 · 스트로브 |
| C 입장권 | 종이 카드 · 절취선 | 가로 두 줄 (검정) | 종이×파랑 | 인쇄 그레인 |
| D 네온 | 어두운 방 · 간판 | 속 빈 튜브 | 검정×형광 초록 | 후광 · 바닥 반사 |
| E 그리드 | 칸으로 분할 | 칸마다 다름 | 검정×오렌지 | 색면 대비 |

**영상은 BPM 이 아니라 소리에 붙입니다.** 처음엔 BPM 으로 박만 계산해 화면을 뛰게 했는데, 규칙적으로 뛸 뿐 곡이 하는 일과 무관해서 "반응이 없다"는 지적을 받았습니다(2026-08). 지금은 wav 를 읽어 **저역(킥)·중역·고역(하이햇)·어택**을 프레임 단위로 뽑고 그 값으로 움직입니다. 드롭에서 커지고 브레이크에서 잦아드는 건 그래서입니다.

포락선을 **최댓값으로 정규화하면 안 됩니다** — 순간 피크 하나에 전체가 눌려 거의 안 움직입니다. 상위 3%(`percentile 97`)를 1.0 으로 잡습니다. 어택은 에너지가 **늘어난 만큼만** 취합니다(줄어드는 구간은 0). 글리치·섬광은 어택에서만 튀게 해야 고장 난 화면으로 안 보입니다.

**글자가 잘리는 두 가지 원인** (2026-08 지적).
① 밴드에서 **접착 탭을 덧칠**해 두 번째 반복의 오른쪽 글자를 덮었습니다. 덩어리 폭은 전체 폭이 아니라 **탭을 뺀 폭**(`PRINT = W - TAB`)에서 나눠야 합니다. 고친 뒤 탭 영역 잉크 0px 을 픽셀로 확인했습니다.
② 여백을 넘겨 흘린 헤드라인. **흘려도 되는 건 마퀴처럼 반복되는 것뿐입니다** — 한 번만 나오는 글자가 여백을 넘으면 잘린 것으로 보입니다.

**마퀴는 낱말 사이에서 잘려야 합니다.** 반복 주기와 화면 폭이 어긋나면 낱말 한가운데가 끊깁니다. 화면 폭에 반복이 정확히 n 번 들어가도록 글자 크기를 역산하고(`repeats=`), 보이는 창의 왼쪽 끝이 반복 경계와 맞도록 위상을 밉니다.

**붙임표 규칙** — 숫자 구간은 붙인 en dash(`19:00–24:00`), 글로 쓴 시간은 띄운 em dash(`오후 7시 — 자정`). 자산마다 다르면 눈에 걸립니다.

**빛을 세게 주면 글자가 사라집니다.** 후광(glow)을 키우면 글자 속까지 밝아져 획이 뭉갭니다 — "LED를 너무 줘서 글씨가 안 보인다"는 지적을 받았습니다(2026-08). **넓은 후광은 옅게, 좁은 후광만 살립니다.** D안 네온도 같은 이유로 넓은 쪽을 0.50 → 0.26 으로 내렸습니다. 밝기를 올리는 대신 배경을 더 눌러야 글자가 삽니다.

**행사가 19시~24시라 전부 밤 톤입니다.** 낮 물색으로 두면 포스터와 실제가 어긋납니다.

**행사 이름은 형식보다 커야 합니다.** 행사명이 `AFTER SUNSET` 으로 정해지면서 넣었는데, 처음엔 `POOL PARTY`·`SOLO PARTY` 를 전폭으로 둔 채 이름만 얹어 이름이 부제처럼 보였습니다. **이름이 브랜드고 형식은 설명입니다** — 형식을 폭 52% 로 줄여 위계를 뒤집었습니다. A안 위쪽 마퀴 띠는 뺐습니다. 물만 있던 빈 자리를 메우려던 장식이었고, 이름이 그 자리를 차지했습니다.

**라벨과 값은 가운데가 아니라 베이스라인을 맞춥니다**(`tmask_bl`/`paint_bl`). 영문 대문자 라벨과 한글 값은 위아래로 튀어나온 정도가 달라서, 같은 y 에 가운데를 맞추면 서로 다른 높이에 뜬 것처럼 보입니다. 타임테이블처럼 크기가 다른 두 글자가 한 줄에 있는 곳에서 이 한 끗이 제일 크게 보입니다.

**타임테이블의 `SOLO PARTY` 는 DJ 가 아니라 프로그램입니다.** 같은 색으로 두면 DJ 이름으로 읽힙니다 — `event.py` 의 `PROGRAM` 에 넣고 강조색으로 갈랐습니다.

**정보를 늘리면 사진 비율도 같이 바꿔야 합니다.** 타임테이블·로고가 들어오면서 아래가 빽빽해졌는데 경계(SEAM)를 그대로 두니 위는 텅 비고 아래는 글자 벽이 됐습니다. A안 경계를 0.46 → 0.36 으로 올려 아래에 자리를 냈습니다(스토리 0.44 → 0.34). **`poster_motion.py` 의 seam 값도 같이 고쳐야 합니다** — 어긋나면 영상에서 마퀴가 경계에서 뜹니다.

**강조색을 같은 계열 배경 위에 쓰지 않습니다.** B안은 배경이 붉은 판이라 붉은 시간 글자가 안 읽혔습니다. 강조색은 구역 라벨에만 남기고 본문은 흰색으로 갔습니다.

**빽빽해지면 장식을 뺍니다.** A안 옆에 세웠던 세로 글자는 타임테이블이 들어오면서 값 열을 뚫고 지나갔습니다. 여백을 채우려고 넣은 것이 정보가 늘자 소음이 됐습니다.

**로고 PNG의 알파는 최솟값으로 판단하면 안 됩니다.** 배경이 불투명한 PNG라도 가장자리 안티에일리어싱 픽셀 하나 때문에 최솟값이 250 밑으로 떨어져, 알파를 믿는 순간 배경까지 통째로 칠해진 **흰 사각형**이 나옵니다(CLUB ACE 로고에서 실제로 발생). **실제로 뚫린 픽셀이 5% 넘게 있을 때만** 알파를 쓰고, 아니면 밝기를 알파로 씁니다. 경계 상자 문턱도 24는 넘겨야 배경 글로우와 바닥 반사가 안 딸려 들어옵니다.

**크기·자간·여백을 눈대중으로 고르지 않습니다.** 크기는 기준 18과 비율 1.30 에서 뽑습니다(18·23·30·40·51) — 서로 관계가 있는 숫자라야 한 판으로 읽힙니다. **자간은 크기에 반비례합니다**: 작은 글자일수록 벌려야 읽히고 큰 글자에 같은 자간을 주면 흩어집니다(18→0.268, 51→0.136). 처음엔 이게 **거꾸로** 들어가 제일 작은 협업 브랜드 줄의 자간이 제일 좁았고, 그래서 25mm 에서 뭉쳐 보였습니다. 여백은 한 단위 `U=16` 의 배수로만 씁니다 — `U` 하나만 바꾸면 판 전체의 숨이 같이 바뀝니다.

**어두운 데서 눈에 걸리는 건 색이 아니라 밝기 대비입니다.** 어두우면 망막의 간상체가 일하는데 간상체는 색을 못 봅니다. 그래서 밴드마다 **밝기 대비가 가장 큰 덩어리(등급 칩)를 딱 하나** 두고 나머지는 조용히 둡니다 — **눈에 띄는 것이 둘이면 둘 다 안 띕니다.**

**밤에는 붉은색이 실제보다 훨씬 어두워 보입니다**(푸르키니에 현상 — 어두워지면 눈의 민감도가 푸른-초록 쪽으로 옮겨 갑니다). VIP 로즈는 바탕색만 믿지 않고 밝은 칩이 대신 눈에 걸리게 했습니다. 금색은 초록 성분이 있어 밤에 덜 죽습니다.

**꼭 보여야 하는 것만 더 자주 반복합니다.** 밴드에서 그건 등급 하나뿐입니다 — 이름은 두 번이면 충분하지만 등급은 어느 각도에서든 잡혀야 합니다. 낱말 없이 **막대만** 한 벌 더 두면 자리를 거의 안 먹고 등급 신호가 네 번 나타납니다. 막대 자리는 **개수와 무관하게 폭을 고정**해야 밴드 네 종의 로고 위치가 안 어긋납니다.

**로고를 나란히 놓을 때 높이를 맞추면 안 됩니다.** 로고마다 획이 굵고 가는 정도가 달라서 같은 높이로 놓으면 굵은 쪽이 훨씬 커 보입니다. **잉크 양(알파 합)** 을 맞추고 높이 상한만 따로 겁니다(`poster_kit.partner_strip`). 색도 한 색으로 통일합니다 — 흰색·금색이 섞이면 협찬 딱지처럼 보입니다.

**타임테이블 여덟 줄은 두 칸으로 접습니다.** 한 줄씩 쌓으면 포스터 절반을 먹습니다.

**포스터가 촌스러워지는 조합** — `poster_split.py` 첫 판이 전단지처럼 나와 반려됐습니다(2026-08). 범인은 넷이고, 하나만 남겨도 티가 납니다.

1. 글자 그림자·외곽선 → 뺍니다. 대비는 사진을 눌러서 만듭니다(`g` 그라디언트).
2. 전부 가운데 정렬 → 좌측 기준선 하나(`M = W*0.088`)로 세웁니다.
3. 노란 알약·노란 CTA 띠 → 없앱니다. 색은 사진과 흰색만.
4. 굵은 한글 볼드(KRB) → 보통 굵기(KR)에 자간을 줍니다.

정보는 문장으로 늘어놓지 말고 **라벨 + 값의 표**로 씁니다(`ROWS`). × 는 오른쪽 끝에 작게 — 가운데에 크게 박는 순간 촌스러워집니다.

**작은 글자는 폭 기준으로 크기를 잡습니다** (`V = W/1080`). 높이 기준(`U = H/1350`)으로 잡으면 스토리(9:16)에서 1.42배로 커져 표 값이 여백을 다 먹고 하단 두 줄이 붙습니다. 높이 기준은 세로 위치와 타이틀에만 씁니다.

**파티 느낌은 장식이 아니라 넷에서 나옵니다** — 위 규칙만 지키면 깔끔하지만 잡지처럼 차분해져서 "파티 느낌으로 리뉴얼" 요청을 받았습니다(2026-08). 더한 것은 이것뿐입니다.

1. **색 충돌** — 물은 시안, 클럽은 마젠타. 한 색으로 통일하면 잡지가 됩니다. 색은 셋(시안·마젠타·흰색)까지만.
2. **눕힌 축** — 경계와 띠를 7도 눕힙니다. 타이틀은 가로로 둡니다. 다 눕히면 산만합니다.
3. **마퀴 띠 두 줄** — 경계에 시안, 물만 있는 빈 자리에 마젠타를 **반대 각도로**. 서로 잡아 주면서 밀도가 생깁니다. 셋 이상은 산만합니다.
4. **번짐(bloom)** — 밝은 곳을 임계값으로 뽑아 블러해 더합니다. 조명이 번져야 클럽입니다.
5. **색분해(RGB split)** — 타이틀 뒤에 마젠타·시안을 어긋나게 깝니다(`paint_split`). 어긋남은 W의 1% 안쪽 — 키우면 고장 난 화면처럼 보입니다.
6. **네온 후광** — 타이틀·훅 뒤에 색을 블러해 **더합니다**(`glow`). 그림자와 정반대입니다. 이게 눈이 제일 확 가는 장치입니다.
7. **빛 알갱이** — 수면에 아나모픽 섬광(`flares`), 클럽 쪽에 보케(`bokeh`). 섬광은 **사방 대칭 별표로 그리면 크리스마스 장식**이 되므로 가로로 2.6배 늘입니다. 밝은 국소 최대점만 골라 6개까지 — 아무 데나 뿌리면 지저분합니다.
8. **여백 디테일** — 옆에 세운 세로 글자, 네 귀퉁이 십자. 정보는 없지만 여백이 "비어 있는" 게 아니라 "비워 둔" 것으로 읽힙니다.

**빛은 누른 다음에 더합니다.** 섬광·보케·후광을 어둡게 누르기 전에 얹으면 같이 죽습니다. 순서가 전부입니다.

**사진을 두 단으로 누릅니다.** 한 번에 다 누르면 디스코볼까지 죽어 파티가 안 보입니다. 타이틀 자리는 사진을 살리고(`d`), 표가 앉는 자리만 확실히 죽입니다(`d2`).

**사진은 캔버스가 아니라 실제로 보이는 띠 높이에 맞춰 자릅니다.** 클럽 사진(가로로 넓음)은 가운데 빈 곳만 잘려 색 덩어리가 됐고, 물 사진은 다이빙대 끝만 삼각형처럼 삐져나와 "애매하다"는 지적을 받았습니다. 띠 높이로 자르고 남는 쪽은 가장자리 줄로 채웁니다(`np.tile`). 물은 `zoom=1.25`, `focus=0.62` 가 보드가 대각선으로 통째로 들어오는 자리입니다.

**CC0는 저작권만 푼 것이고 초상권은 별개입니다.** `club-cc0.jpg`는 관객 얼굴이 다 나와서 얼굴이 없는 위 34%만 씁니다(`zoom=2.9`, `focus=0.16`). 이 값은 올리기만 하세요.

**글로우 처리 주의** — 마스크를 그대로 블러하면 배열 가장자리에서 잘려 사각 후광이 생깁니다. `cv2.copyMakeBorder`로 패딩한 뒤 블러합니다(`render.py`의 `blit`).
**반전 컷**에서 alpha가 음수로 들어오므로 `blit`의 조기 반환은 `abs(alpha)`로 판정합니다.

---

## 5. 인스타 콘텐츠

카드뉴스는 **AROS · LYNN · V · TS** 4명분(각 5장)이 있습니다.
캡션과 인스타 소개글도 함께 작성했습니다. 캡션 요청 시 사용자가 강조한 조건: **"AI 말투로 하면 절대 안 돼."**

---

## 6. 제안서

`proposal/`(웨이비 스튜디오, 18p) · `proposal2/`(디제이코리아, 15p). HTML을 `@page { size: A4 landscape }`로 짜고 Chrome headless로 PDF를 뽑습니다.

### 웨이비 스튜디오 — 건대점
레슨과 **연습실 대여를 함께** 운영합니다. 처음엔 학원으로만 보고 썼다가, 확인 후 연습실 이용자를 협업 대상에 넣고 상대를 "학원"으로 단정한 표현 세 곳을 고쳤습니다.
제안 골자: 오픈덱 나이트 정례화 → 수강생·수료생·연습실 이용자를 씬에 붙잡는 리텐션 장치.

### 디제이코리아
확인된 사실: Pioneer DJ 독점 공식 수입, Allen & Heath · UDG 유통, 용산 아이파크몰 쇼룸, 렌탈, CAMPUS, DJKOREAMALL.
**미검증 2건** — "쇼룸은 이미 사람이 지나다니는 자리"(유동인구 추정), "CAMPUS가 다루기 어려운 부분"(커리큘럼 범위 모름). 미팅에서 확인 후 조정 권장.

> 초안에 "AIAIAI 공식 유통망"이라고 썼다가 사용자가 잡아냈고, 그 뒤 전체 미검증 주장을 감사했습니다. **상대 회사 정보는 반드시 교차 확인하고, 확인 안 되면 쓰지 않습니다.**

---

## 7. 과거에 거부·폐기된 방식

같은 실수를 반복하지 않기 위한 목록입니다.

| 시도 | 결과 |
|---|---|
| 로고를 SVG로 다시 그림 | **거부** — "로고가 전혀 디자인이 다른데?" 원본 PNG를 잘라 쓰는 방식으로 전환 |
| 멤버별 사진 크기(`fit`) 개별 조정 | **거부** — "전부 똑같이 사이즈 맞춰주는데". CSS 한 줄로 통일 |
| GitHub Pages 배포 | **폐기** — 이미 Vercel로 서비스 중이었습니다. Actions 워크플로 삭제함 |
| 스톡 사진 사용 | 처음부터 배제. 캔버스 생성 아트로 대체 |

**접속 차단 (2026-08-04 ~)**
`vercel.json`이 모든 요청을 `maintenance.html`로 보내고 있습니다. **되돌리려면 `vercel.json`을 지우고 푸시하면 끝**입니다. 사이트 코드는 하나도 안 건드렸습니다.
- `rewrites`로는 안 막힙니다. Vercel은 **redirects → 정적 파일 → rewrites** 순서라 `index.html`처럼 실제로 있는 파일은 rewrites를 안 탑니다. 파일시스템보다 먼저 도는 **legacy `routes`**를 써야 합니다.
- **`vercel.json`에 `"//"` 같은 주석 키를 넣으면 배포가 통째로 실패합니다.** 스키마가 추가 속성을 거부합니다. 설명은 이 문서에 씁니다.

**기술적으로 물렸던 것**
- **가로선은 비네팅 뒤에 그려야 한다.** 앞에 그리면 타일 가장자리가 어두워져서, 그리드에 붙였을 때 선이 이어지지 않고 끊긴 점선처럼 보인다. 높이만 맞춰서는 안 이어진다.
- **피드 자산의 가로선은 전부 y=1058이어야 한다.** 그리드에서 옆 칸과 이어져 보이는 유일한 장치다. 한동안 `cards.py`에는 선이 아예 없었고 `feed_grid.py`는 760·900·960·1060으로 흩어져 있어서 줄이 끊겨 보였다. 지금은 `feed.py` · `feed2.py` · `feed_row.py` · `feed_grid.py` · `cards.py` 모두 1058. **새 자산을 만들면 이 값을 그대로 쓸 것.**
- **릴스 커버는 9:16으로 올라가고, 그리드에서는 그 가운데를 4:5로 잘라 보여준다.** 1080×1350 타일을 그대로 커버로 주면 인스타가 맞추면서 가로선 위치가 밀린다. `feed_row.py`의 `cover()`가 1080×1920 캔버스 가운데에 타일을 놓아 `{key}_3_cover.png`를 만든다. 이걸 커버로 지정해야 카드와 선이 맞는다.
- **인스타 피드 그리드는 타일 사이에 간격이 있고, 썸네일로 크게 줄어든다.** 경계에 걸친 요소는 그만큼 잘리고 가는 획은 아예 안 보인다. 엠블럼(가운데 번개)과 UNDERGROUND의 G가 실제로 사라졌다. 규칙: **글자는 경계에 절대 올리지 않는다**(`feed2.py`의 `SEAM_CLEAR`). 엠블럼처럼 경계에 걸쳐야 하는 것은 **획이 두껍게 보이도록 아주 크게**(`LOGO_H = 860` 이상) 쓴다. 작업 후 반드시 타일 폭 305px로 줄여 썸네일에서 읽히는지 확인할 것.
- `.member__photo img`(0,1,1)가 `.member__cut`(0,1,0)를 이겨서 크기 조정이 안 먹었습니다 → 선택자를 `.member__photo img.member__cut`로.
- 제안서 페이지 번호를 파이썬으로 일괄 수정하다 `<head>`를 통째로 날린 적이 있습니다. **HTML을 문자열 분할로 재조립하지 마세요.**
- 윈도우에서 `python -c`로 `—` 출력 시 cp949 오류 → `PYTHONIOENCODING=utf-8`.
- 카카오톡 링크 미리보기가 빈 화면 → og:image를 **절대 URL**로. 카카오는 캐시하므로 디버거로 초기화하거나 `?v=2`를 붙여 확인.
